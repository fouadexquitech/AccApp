import { ScrollNearEnd.DirectiveDirective } from './scroll-near-end.directive.directive';

describe('ScrollNearEnd.DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollNearEnd.DirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
